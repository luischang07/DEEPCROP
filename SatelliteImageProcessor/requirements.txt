# Librerías principales para el backend web
Flask==3.0.0         # Framework web ligero para crear APIs y manejar rutas HTTP
Flask-CORS==4.0.0    # Extensión de Flask para permitir solicitudes CORS
werkzeug>=3.0.1      # Utilidades web que Flask usa internamente (WSGI, requests/responses, debugging)
python-dotenv>=1.0.0 # Permite cargar variables de entorno desde un archivo .env

# Librerías para procesamiento de imágenes y datos
numpy>=1.26.0        # Manejo de arrays y operaciones numéricas, fundamental para procesar bandas de imágenes
rasterio>=1.3.9      # Leer y escribir imágenes raster geoespaciales (TIFF, GeoTIFF) y acceder a metadatos
Pillow>=10.1.0       # Manipulación de imágenes (abrir, guardar, redimensionar, convertir formatos)
matplotlib>=3.8.0    # Visualización de datos e imágenes, histogramas, gráficos

# Librerías para networking y distribución
requests>=2.31.0     # Realizar peticiones HTTP a APIs externas o descarga archivos
setuptools>=68.0.0   # Herramienta para empaquetar y distribuir librerías Python (necesario para instalar dependencias)
